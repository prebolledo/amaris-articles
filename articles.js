let articles = []

articles.push({
    id: articles.length + 1,
    name:'Taza'
})

articles.push({
    id: articles.length + 1,
    name:'Plato'
})

articles.push({
    id: articles.length + 1,
    name:'Cuchara'
})

articles.push({
    id: articles.length + 1,
    name:'Tenedor'
})

articles.push({
    id: articles.length + 1,
    name:'Olla'
})

export default articles